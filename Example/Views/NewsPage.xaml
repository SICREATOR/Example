<Page x:Class="Example.Views.NewsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Example.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="NewsPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <Border Grid.Row="0" Background="#E3F2FD" Padding="10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="AddNewsButton" 
                       Content="Добавить новость" 
                       Padding="10,5"
                       Click="AddNewsButton_Click"/>
                <Button x:Name="RefreshButton" 
                       Content="Обновить" 
                       Padding="10,5"
                       Margin="10,0"
                       Click="RefreshButton_Click"/>
            </StackPanel>
        </Border>

        <!-- Список новостей -->
        <DataGrid x:Name="NewsGrid" 
                 Grid.Row="1" 
                 AutoGenerateColumns="False"
                 CanUserAddRows="False"
                 IsReadOnly="True"
                 SelectionMode="Single"
                 Margin="10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Заголовок" Binding="{Binding Title}" Width="200"/>
                <DataGridTextColumn Header="Описание" Binding="{Binding Description}" Width="300"/>
                <DataGridTextColumn Header="Изображение" Binding="{Binding ImagePath}" Width="150"/>
                <DataGridTemplateColumn Header="Действия" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Изменить" 
                                       Margin="2" 
                                       Padding="5,2"
                                       Click="EditNews_Click"/>
                                <Button Content="Удалить" 
                                       Margin="2" 
                                       Padding="5,2"
                                       Click="DeleteNews_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
