<Window x:Class="Example.Views.NewsEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example.Views"
        mc:Ignorable="d"
        Title="NewsEditDialog" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Заголовок:" Grid.Row="0" Margin="0,0,0,5"/>
        <TextBox x:Name="TitleTextBox" Grid.Row="1" Height="25" Margin="0,0,0,10"/>

        <TextBlock Text="Описание:" Grid.Row="2" Margin="0,0,0,5"/>
        <TextBox x:Name="DescriptionTextBox" 
                Grid.Row="3" 
                Height="100"
                TextWrapping="Wrap"
                AcceptsReturn="True"
                VerticalScrollBarVisibility="Auto"
                Margin="0,0,0,10"/>

        <TextBlock Text="Путь к изображению:" Grid.Row="4" Margin="0,0,0,5"/>
        <TextBox x:Name="ImagePathTextBox" Grid.Row="5" Height="25" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="SaveButton" 
                   Content="Сохранить" 
                   Width="100" 
                   Height="30"
                   Margin="5"
                   Click="SaveButton_Click"/>
            <Button x:Name="CancelButton" 
                   Content="Отмена" 
                   Width="100" 
                   Height="30"
                   Margin="5"
                   Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
